# Fixture project goal

Estimate a shoreline boundary from a small raster tile while preserving the
visible boundary shape at the review scale. The result should be stable enough
to compare against a manually marked reference.
