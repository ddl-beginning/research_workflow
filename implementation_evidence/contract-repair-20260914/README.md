# Workflow V2.1 contract-repair evidence

This evidence records the bounded `ACQUISITION_REALISM_BRIDGE_S4_S7` hardening run on 2026-09-14.

- `stage-propagation-audit.json` records the canonical `stage.v2` boundary, four-layer omission diagnostic, fresh-supervisor handshake, and minimal register → plan → start regression.
- `self-repair-e2e.json` records one real facade `START` request that omitted `request.stage`, the single bounded Engine-local repair, and the resulting Stage transition.

The business project and benchmark were treated as frozen. The only project-side state transition was the Workflow-owned `.workflow-v2/journal.json` Stage journal update required to resume the existing Stage; no facade research execution was performed.
